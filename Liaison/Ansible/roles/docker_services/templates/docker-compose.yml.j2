# Docker Compose for Liaison Services
version: '3'
services:
{% for service in docker_services %}
  {{ service.key }}:
    image: {{ service.image }}
    ports:
      - "{{ service.ports }}"
    cap_add:
      - {{ service.caps | default('NET_ADMIN') }}
{% endfor %}